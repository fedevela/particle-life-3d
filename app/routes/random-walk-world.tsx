import { RandomWalkWorldPage } from "~/features/3d/random-walk-world/random-walk-world-page";

/** Issue #32 route placement mapping: CH-001. */
export default function RandomWalkWorldRoute() {
  return <RandomWalkWorldPage />;
}
