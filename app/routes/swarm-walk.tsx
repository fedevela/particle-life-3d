import { SwarmWalkPage } from "~/features/3d/swarm-walk-page";

/**
 * Render the swarm-walk route.
 *
 * @returns Returns the swarm-walk scene page.
 */
export default function SwarmWalkRoute() {
  return <SwarmWalkPage />;
}
